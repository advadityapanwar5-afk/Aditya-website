import { MetadataRoute } from 'next';

export default function robots(): MetadataRoute.Robots {
  // Update domain here too if changed
  const baseUrl = 'https://www.iralawoffice.in'; 

  return {
    rules: {
      userAgent: '*',
      allow: '/',
    },
    sitemap: `${baseUrl}/sitemap.xml`,
  };
}
